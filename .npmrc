# Fix for Rollup platform-specific dependencies
# This helps npm properly handle optional dependencies during deployment
legacy-peer-deps=false
strict-peer-deps=false
auto-install-peers=true
shamefully-hoist=true
# Force npm to install all optional dependencies
optional=true
# Ensure platform-specific binaries are handled correctly
os=linux
cpu=x64
libc=glibc